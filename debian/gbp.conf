[DEFAULT]
# don't check if debian-branch == current branch:
ignore-branch = True
# the default branch for the debian patch:
debian-branch=snapshot/debian/jessie
# the default tag formats used:
debian-tag=master/debian/jessie/%(version)s
upstream-tag=zfs-0.6.5.1
