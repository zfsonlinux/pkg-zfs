[DEFAULT]
# don't check if debian-branch == current branch:
ignore-branch = True
# the default branch for the debian patch:
debian-branch=snapshot/debian/wheezy
# the default tag formats used:
debian-tag=snapshot/debian/wheezy/%(version)s
upstream-tag=zol/master
# the default branch for upstream sources:
upstream-branch=zol/master
